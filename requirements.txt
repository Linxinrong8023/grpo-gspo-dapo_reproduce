# ──────────────────────────────────────────────────────────────────────────────
# post_training_contrast — 服务器安装依赖
#
# 安装方式：
#   pip install -r requirements.txt
#
# 注意：
#   torch 版本必须和服务器 CUDA 版本对应，下面的版本适配 CUDA 12.1+
#   如果是 CUDA 11.8，把 torch 版本替换为 2.1.x 并去掉 +cu121 后缀
#   vllm 对 torch 版本非常敏感，必须配套使用
# ──────────────────────────────────────────────────────────────────────────────

# ── 核心训练框架 ───────────────────────────────────────────────────────────────
torch==2.4.0
transformers==4.45.2
accelerate==0.33.0          # 多 GPU 分布式训练封装
peft==0.11.1                # LoRA / QLoRA 轻量微调（use_lora=True 时必须）
# FlashAttention-2 是可选加速项；训练配置默认 attn_implementation="auto"：
# 已安装 flash_attn 时使用 flash_attention_2，否则自动回退到 sdpa。
# 如需安装，优先下载匹配 wheel 后 pip install，源码编译通常很慢。

# ── 高性能推理（rollout 生成）────────────────────────────────────────────────
# 绝对锁定版本，防止依赖回溯陷阱
vllm==0.5.4

# ── 数学答案等价判断 ────────────────────────────────────────────────────────
sympy==1.13.1
latex2sympy2==1.9.1         # LaTeX 数学表达式 → SymPy 解析
func-timeout==4.3.5         # 单线程 reward 判分中隔离 latex2sympy/sympify 卡死

# ── 数据处理 ────────────────────────────────────────────────────────────────
numpy>=1.24.0
tqdm>=4.66.0                # 进度条（evaluator 批量生成时用）

# ── 监控 ─────────────────────────────────────────────────────────
wandb>=0.17.0               # 训练监控（wandb_enabled=false 时不会被 import）

# ── 可视化 ───────────────────────────────────────────────────────────────────
matplotlib>=3.7.0

# ── 分布式训练（DeepSpeed 支持）────────────────────────────────────────
deepspeed==0.14.4         # 支持 ZeRO-3 全参微调
